---
{% if ansible_os_family == "RedHat" %}
command:
  rpm --verify -a --nofiles -v:
    exit-status: 0
    timeout: 180000
{% endif %}
{% if ansible_os_family == "Debian" %}
command:
  debsums -x:
    exit-status: 0
    timeout: 180000
{% endif %}
